# Used by `docker compose up`. Copy to `.env` (never commit it).
# Get a key at https://console.anthropic.com/ - without one, the AI Assistant
# gives demo answers instead of live Claude replies.
ANTHROPIC_API_KEY=
