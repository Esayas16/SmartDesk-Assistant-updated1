{
  "name": "smartdesk-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Backend API for SmartDesk Assistant. Persists the knowledge base, diagnostic history and settings in PostgreSQL, and proxies AI requests so no API key ever ships to the browser.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/db/migrate.js",
    "seed": "node src/db/seed.js",
    "db:setup": "npm run migrate && npm run seed"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.13.1"
  }
}
